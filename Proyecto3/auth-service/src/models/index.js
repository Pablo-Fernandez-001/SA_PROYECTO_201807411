/**
 * Auth Service Models - SOA Pattern
 * Export all models for the authentication service
 */

const User = require('./User');
const Role = require('./Role');

module.exports = {
  User,
  Role
};
