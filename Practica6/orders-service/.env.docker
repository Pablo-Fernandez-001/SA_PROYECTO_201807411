# Orders Service Environment Variables (Docker)
PORT=3003
NODE_ENV=production

# Database — points to Docker network hostname
DB_HOST=orders-db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=orders_db

# gRPC — catalog service address for validation (Docker network)
CATALOG_GRPC_URL=catalog-service:50052

# RabbitMQ — message broker for async events
RABBITMQ_URL=amqp://delivereats:rabbitmq2024@rabbitmq:5672

# Logging
LOG_LEVEL=info

# Services
NOTIFICATION_SERVICE_URL=http://notification-service:3005
AUTH_GRPC_URL=auth-service:50051
