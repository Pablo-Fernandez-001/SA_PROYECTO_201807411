# Orders Service Environment Variables (local dev)
PORT=3003
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3308
DB_USER=root
DB_PASSWORD=password
DB_NAME=orders_db

# gRPC — catalog service address for validation
CATALOG_GRPC_URL=localhost:50052

# Logging
LOG_LEVEL=info
