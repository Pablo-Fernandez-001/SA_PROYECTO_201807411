# Auth Service Environment Variables for Docker
PORT=50051
NODE_ENV=production

# Database Configuration for Docker
DB_HOST=auth-db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=auth_db

# JWT Configuration
JWT_SECRET=delivereats_super_secret_jwt_key_2024
JWT_EXPIRES_IN=24h

# Logging
LOG_LEVEL=info